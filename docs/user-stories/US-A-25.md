# US-A-25: Управлять API-токенами для интеграций (создавать, отзывать, ограничивать права)

## Цель story
Администратор должен уметь управлять API-токенами для интеграций (создавать, отзывать, ограничивать права), чтобы безопасно подключать внешние системы.

Эпик: `Техническое обслуживание и поддержка`.
Контур: `MVP`.
Приоритет: `Высокий`.
Текущий статус: `Ожидание`.

## Шаги реализации
1. Раздел «API»: список токенов (название, дата создания, последний использован, scope).
   Статус: ожидание.
   Контур: MVP.
   Что включено: Раздел «API»: список токенов (название, дата создания, последний использован, scope).

2. Создание токена: указать название, выбрать разрешения (например, только courses:read).
   Статус: ожидание.
   Контур: Развитие 1 очереди.
   Что включено: Создание токена: указать название, выбрать разрешения (например, только courses:read).

3. Сгенерированный токен показывается один раз.
   Статус: ожидание.
   Контур: Развитие 2 очереди.
   Что включено: Сгенерированный токен показывается один раз.

4. Кнопка «Отозвать» – токен мгновенно перестаёт работать.
   Статус: ожидание.
   Контур: Развитие 2 очереди.
   Что включено: Кнопка «Отозвать» – токен мгновенно перестаёт работать.

## Что считать готовностью story
- Раздел «API»: список токенов (название, дата создания, последний использован, scope).

## Возможные следующие улучшения, но не блокеры для story
- Создание токена: указать название, выбрать разрешения (например, только courses:read). Контур: `Развитие 1 очереди`.
- Сгенерированный токен показывается один раз. Контур: `Развитие 2 очереди`.
- Кнопка «Отозвать» – токен мгновенно перестаёт работать. Контур: `Развитие 2 очереди`.
