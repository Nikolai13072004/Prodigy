# US-HR-14: Импортировать список сотрудников из CSV (email, имя, отдел) для массового создания учеников и, опционально, автоматической записи на обязательные курсы

## Цель story
HR должен уметь импортировать список сотрудников из CSV (email, имя, отдел) для массового создания учеников и, опционально, автоматической записи на обязательные курсы, чтобы решать рабочую задачу без лишних ручных действий.

Эпик: `Управление пользователями (ограниченное)`.
Контур: `Не использовать`.
Приоритет: `Высокий`.
Текущий статус: `Ожидание`.

## Шаги реализации
1. Раздел «Импорт пользователей»: загрузка CSV с колонками email, first_name, last_name, department.
   Статус: ожидание.
   Контур: Не использовать.
   Что включено: Раздел «Импорт пользователей»: загрузка CSV с колонками email, first_name, last_name, department.

2. Опция: «После импорта назначить на курсы» – выбор одного или нескольких курсов.
   Статус: ожидание.
   Контур: Не использовать.
   Что включено: Опция: «После импорта назначить на курсы» – выбор одного или нескольких курсов.

3. Предпросмотр и подтверждение.
   Статус: ожидание.
   Контур: Не использовать.
   Что включено: Предпросмотр и подтверждение.

4. Отчёт об импорте: создано новых, обновлено (по email), ошибки.
   Статус: ожидание.
   Контур: Не использовать.
   Что включено: Отчёт об импорте: создано новых, обновлено (по email), ошибки.

## Что считать готовностью story
- Раздел «Импорт пользователей»: загрузка CSV с колонками email, first_name, last_name, department.
- Опция: «После импорта назначить на курсы» – выбор одного или нескольких курсов.
- Предпросмотр и подтверждение.
- Отчёт об импорте: создано новых, обновлено (по email), ошибки.

## Возможные следующие улучшения, но не блокеры для story
- Дополнительные улучшения сейчас не выделены отдельно от основного контура story.
